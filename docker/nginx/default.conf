upstream express{
    # docker compose に合わせる
    server express:3000;
}

server {
    listen 80 default;
    server_name _;

    location / {
        proxy_pass http://express;
    }

    location /static {
        autoindex on;
        alias /static;
    }
}
